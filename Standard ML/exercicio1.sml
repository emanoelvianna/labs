(* questão 1 *)
open Math;
fun isNumber(number, []) = isNumber(numeber, x::xs) = if number = x then true else x::(remover(number,xs));

fun isNumber(number, []) =
	let 
		val maior = maior2 (a,b)
		val menor = a + b - maior
	in
		if maior mod menor = 0 then true
		else false
	end;

